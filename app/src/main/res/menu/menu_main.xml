<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".MainActivity">
    <item android:id="@+id/action_battery_20"
        android:icon="@mipmap/ic_battery_20"
        android:visible="false"
        app:showAsAction="ifRoom"  />
    <item android:id="@+id/action_battery_50"
        android:icon="@mipmap/ic_battery_50"
        android:visible="false"
        app:showAsAction="ifRoom"  />
    <item android:id="@+id/action_battery_70"
        android:icon="@mipmap/ic_battery_70"
        android:visible="false"
        app:showAsAction="ifRoom"  />
    <item android:id="@+id/action_battery_71"
        android:icon="@mipmap/ic_battery_71"
        android:visible="false"
        app:showAsAction="ifRoom"  />
</menu>
